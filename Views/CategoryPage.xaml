<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MauiApp1.Views.CategoryPage"
             Title="{Binding Title}">
    
    <StackLayout>


        <Label Text="Title" FontSize="Medium" />
        <Entry Text="{Binding Title}" FontSize="Small" />
    
        <Label Text="Description" FontSize="Medium" />
        <Entry Text="{Binding Description}" FontSize="Small" />

        <Label Text="Success Rate" FontSize="Medium" />
        <Entry Text="{Binding SuccessRate}" FontSize="Small" />



        <StackLayout Orientation="Horizontal">
            <Button Text="Cancel" Command="{Binding CancelCommand}" HorizontalOptions="FillAndExpand"></Button>
            <Button Text="Save" Command="{Binding SaveCommand}" HorizontalOptions="FillAndExpand"></Button>
        </StackLayout>
    </StackLayout>
</ContentPage>